{
  "name": "quickthoughts",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Starter",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate",
    "deploy": "npm run build && gh-pages publish -d www"
  },
  "dependencies": {
    "@stencil/core": "^1.3.3"
  },
  "license": "MIT",
  "devDependencies": {
    "@stencil/sass": "^1.1.0",
    "chokidar": "^3.2.2",
    "gh-pages": "^2.1.1"
  }
}
